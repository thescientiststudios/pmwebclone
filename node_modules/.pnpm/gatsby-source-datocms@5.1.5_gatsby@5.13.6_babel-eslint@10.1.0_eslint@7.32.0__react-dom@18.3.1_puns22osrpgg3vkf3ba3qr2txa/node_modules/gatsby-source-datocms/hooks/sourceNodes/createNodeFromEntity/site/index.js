"use strict";

var buildNode = require('../utils/buildNode');

var _require = require('datocms-client'),
    faviconTagsBuilder = _require.faviconTagsBuilder;

module.exports = function buildSiteNode(entity, _ref) {
  var entitiesRepo = _ref.entitiesRepo,
      generateType = _ref.generateType;
  var siteNode = buildNode(generateType('Site'), entity.id, function (node) {
    node.entityPayload = entity.payload;
    node.faviconMetaTags = faviconTagsBuilder(entitiesRepo);
  });
  return [siteNode];
};